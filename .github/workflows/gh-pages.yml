name: Deploy MkDocs to GitHub Pages
on:
  push:
    branches: [master] # change to 'main' if your repo uses main

concurrency:
  group: gh-pages-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs mkdocs-material mkdocs-document-dates

      - name: Build site
        run: mkdocs build --clean

      - name: Show built site files (debug)
        run: |
          if [ -d "./site" ]; then
            echo "site/ exists — contents:"
            ls -la ./site || true
          else
            echo "site/ directory missing!"
            exit 1
          fi

      - name: Configure Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact (site) for Pages (v4)
        # switch to v4 so it uses upload-artifact@v4 under the hood
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./site
          # (optional) name: github-pages     # default is 'github-pages'

  deploy:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages (v4)
        uses: actions/deploy-pages@v4
